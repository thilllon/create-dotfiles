pre-commit:
  parallel: true
  commands:
    format:
      run: pnpm format
      stage_fixed: true

pre-push:
  parallel: true
  commands:
    typecheck:
      run: pnpm typecheck
    build:
      run: pnpm build
    test:
      run: pnpm test
