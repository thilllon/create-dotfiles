# esbuild (a transitive build dependency) runs a postinstall script to fetch its platform
# binary. pnpm >= 11 exits non-zero on unapproved build scripts, so approve it explicitly.
allowBuilds:
  esbuild: true
